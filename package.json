{
  "dependencies": {
    "concurrently": "^6.0.0",
    "nodemon": "^2.0.7"
  },
  "scripts": {
    "client": "cd client && yarn start",
    "server": "nodemon server/server.js",
    "runall": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  }
}